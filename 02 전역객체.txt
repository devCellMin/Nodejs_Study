console과 같이 load를 하지 않고 바로 사용할 수 있는 객체를 전역객체라고 한다.

전역 모듙(객체) : 프로그램 전체에서 사용하는 객체
전역 변수 : 프로그램 전체에서 사용하는 변수
(__filename, __dirname)

변수 - var, let
상수 - const
리터럴 - 순수 값(숫자, 문자, Bool, null, undefined, symbol, ...)

객체의 변수 : 속성 (Properties)
객체의 함수 : 메서드 (Method)

Console 객체
Process 객체
OS