<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>EJS EX01</title>
        <% let test = "좋은날"; %>
    </head>
    <body>
        <h1><%= test %></h1>
        <hr>
        <% for(let i = 0; i < 10; i++) { %>
            <p><%= `${i}. ${test} ${i}` %></p>
        <% } %>
    </body>
</html>